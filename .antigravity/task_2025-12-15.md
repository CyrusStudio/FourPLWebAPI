# SO 資料同步服務重構

## 任務目標
將舊版 GetSO 程式碼重構至新架構，使用 `ISapHelper` 和 `ISqlHelper` 等基礎設施服務。

## 任務清單

### 規劃階段
- [x] 分析現有架構模式
- [x] 撰寫實作計畫
- [x] 等待使用者審核

### 實作階段
- [x] 建立 `SOMasterData` DTO 模型
- [x] 建立 `SOQueryRequest` 請求模型
- [x] 擴展 `ISqlHelper` 支援 Bulk Insert
- [x] 建立 `ISOService` 介面與 `SOService` 實作
- [x] 在 `IntegrationController` 加入 SO 同步端點
- [x] 註冊服務至 DI 容器

### 驗證階段
- [x] 編譯測試
- [ ] 手動 API 測試 (由使用者驗證)
